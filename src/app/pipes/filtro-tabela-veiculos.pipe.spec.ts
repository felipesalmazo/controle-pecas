import { FiltroTabelaVeiculosPipe } from './filtro-tabela-veiculos.pipe';

describe('FiltroTabelaVeiculosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTabelaVeiculosPipe();
    expect(pipe).toBeTruthy();
  });
});
